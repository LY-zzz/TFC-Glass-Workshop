# TFC Glass Workshop 项目续接摘要

## 项目基础

- 项目：Minecraft Forge `1.20.1` + TerraFirmaCraft:TNG 附属 Mod
- 名称：`TerraFirmaCraft: Glass Workshop`
- Mod ID：`tfc_glass_workshop`
- Java 包：`com.ly.tfcglassworkshop`
- GitHub：`https://github.com/LY-zzz/TFC-Glass-Workshop.git`
- 当前分支：`main`
- 当前远端最新已推送提交：`0c0bd85 Add bottle recipes for other glass batches`
- 本地仍有未提交改动：模具可堆叠修正

## 环境

- 必须使用 JDK 17
- 常用命令：

```powershell
$env:JAVA_HOME='C:\Program Files\Java\jdk-17'
$env:PATH="$env:JAVA_HOME\bin;$env:PATH"
$env:GRADLE_USER_HOME='D:\java\TFC-Glass-Workshop\.gradle-home'
.\gradlew.bat processResources compileJava
.\gradlew.bat runClient
```

- `runClient` 的 Patchouli mixin refmap 问题已修：
  - `mixin.env.remapRefMap=true`
  - `mixin.env.refMapRemappingFile=${projectDir}/build/createSrgToMcp/output.srg`
- Patchouli runtime 固定为 `1.20.1-84.1-FORGE`
- TFC：CurseForge file id `7730077`，版本范围 `[3.2.21,)`

## 已完成基础结构

- 已统一项目身份：
  - 主类：`TFCGlassWorkshop`
  - 包名：`com.ly.tfcglassworkshop`
  - Mod ID：`tfc_glass_workshop`
  - `mods.toml` 已声明 Forge、Minecraft、TFC 硬依赖
- 已建立注册结构：
  - `ModBlocks`
  - `ModItems`
  - `ModCreativeTabs`
  - `ModBlockEntities`
  - `ModMenuTypes`
  - `ModRecipeTypes`
  - `ModTags`

## Glass Press

- 方块：`glass_press`
- 类：
  - `GlassPressBlock`
  - `GlassPressBlockEntity`
  - `GlassPressMenu`
  - `GlassPressScreen`
- 行为：
  - 水平朝向 `FACING`
  - 放置朝向玩家相反方向
  - 支持旋转/镜像
  - 右键打开容器菜单
  - 服务端 ticker
  - 方块破坏掉落内部库存
- GUI：
  - 代码绘制临时界面
  - 当前只显示机器槽位和进度条
  - 温度文本/状态文本已移除
  - 玩家物品栏槽位逻辑存在，但背景格子未正式贴图化

## 槽位约定

- `INPUT_SLOT = 0`
- `MOLD_SLOT = 1`
- `OUTPUT_SLOT = 2`
- `SLOT_COUNT = 3`
- 输入槽：不接受模具
- 模具槽：只接受 `tfc_glass_workshop:press_molds`，最大 1 个
- 输出槽：禁止手动放入
- Shift-click：
  - 模具优先进模具槽
  - 其他物品进输入槽

## 压制逻辑

- `PRESS_TIME = 20` tick
- 每 tick 服务端检查：
  - 输入非空
  - 模具非空
  - 匹配 `tfc_glass_workshop:pressing`
  - 输入有 TFC `HeatCapability`
  - 输入温度 `>= min_temp`
  - 输出槽为空，或同物品同标签且可堆叠
- 完成后：
  - 输入消耗 1 个
  - 金属模具不消耗
  - 陶瓷模具有概率碎裂
  - 输出写入/累加到输出槽
- 温度 API：
  - `HeatCapability.has(stack)`
  - `HeatCapability.getTemperature(stack)`

## glass_batch 加热

- TFC 原版 `glass_batch` 原本不能进碳炉/坩埚，因为缺 `item_heats`
- 已新增：`src/main/resources/data/tfc/tfc/item_heats/glass_batches_tfc_glass_workshop.json`

```json
{
  "ingredient": { "tag": "tfc:glass_batches" },
  "heat_capacity": 0.7
}
```

- 覆盖：
  - `tfc:silica_glass_batch`
  - `tfc:hematitic_glass_batch`
  - `tfc:olivine_glass_batch`
  - `tfc:volcanic_glass_batch`

## 配方系统

- 自定义配方类型：`tfc_glass_workshop:pressing`
- 类：`PressingRecipe`
- JSON 字段：

```json
{
  "type": "tfc_glass_workshop:pressing",
  "input": { "item": "..." },
  "mold": { "tag": "..." },
  "min_temp": 900.0,
  "result": {
    "item": "...",
    "count": 1
  }
}
```

- 已支持：
  - JSON 读取
  - 网络同步
  - 输入 + 模具匹配
  - `getResult()` 返回 copy

## 模具系统

- 已有 18 个模具：
  - 铁/基础模具 6 个：`plate_mold`、`block_mold`、`bottle_mold`、`jar_mold`、`lamp_glass_mold`、`lens_mold`
  - 钢模具 6 个：`steel_plate_mold`、`steel_block_mold`、`steel_bottle_mold`、`steel_jar_mold`、`steel_lamp_glass_mold`、`steel_lens_mold`
  - 陶瓷模具 6 个：`ceramic_plate_mold`、`ceramic_block_mold`、`ceramic_bottle_mold`、`ceramic_jar_mold`、`ceramic_lamp_glass_mold`、`ceramic_lens_mold`
- 标签：
  - `press_molds`
  - `plate_molds`
  - `block_molds`
  - `bottle_molds`
  - `jar_molds`
  - `lamp_glass_molds`
  - `lens_molds`
- 配方按形状 tag 匹配，不写死单个模具
- 铁/钢模具永久使用
- 陶瓷模具：
  - 类：`CeramicMoldItem`
  - 当前碎裂概率：`0.1F`
  - 逻辑位置：`GlassPressBlockEntity#damageMold`
  - 已改成原版 TFC 风格的概率碎裂，不再使用耐久
- 最新本地未提交修正：
  - `ModItems#registerMold` 去掉 `stacksTo(1)`
  - `CeramicMoldItem` 去掉 `stacksTo(1)`
  - 原因：TFC 原版空模具可堆叠，只有装内容的模具限制单个
  - 编译已通过：`.\gradlew.bat processResources compileJava`

## 当前已存在压制配方

目录：`src/main/resources/data/tfc_glass_workshop/recipes/pressing/`

硅玻璃完整 6 条：

- `silica_plate_mold.json`
  - 输入：`tfc:silica_glass_batch`
  - 模具：`#tfc_glass_workshop:plate_molds`
  - 输出：`minecraft:glass_pane` x16
- `silica_block_mold.json`
  - 输出：`minecraft:glass`
- `silica_bottle_mold.json`
  - 输出：`tfc:silica_glass_bottle` x1
- `silica_jar_mold.json`
  - 输出：`tfc:empty_jar` x4
- `silica_lamp_glass_mold.json`
  - 输出：`tfc:lamp_glass`
- `silica_lens_mold.json`
  - 输出：`tfc:lens`

其他瓶类配方已补齐并已推送：

- `hematitic_bottle_mold.json`
  - 输入：`tfc:hematitic_glass_batch`
  - 输出：`tfc:hematitic_glass_bottle` x1
- `olivine_bottle_mold.json`
  - 输入：`tfc:olivine_glass_batch`
  - 输出：`tfc:olivine_glass_bottle` x1
- `volcanic_bottle_mold.json`
  - 输入：`tfc:volcanic_glass_batch`
  - 输出：`tfc:volcanic_glass_bottle` x1

通用参数：

- `min_temp = 900.0`
- 瓶模具：`#tfc_glass_workshop:bottle_molds`

## TFC 玻璃瓶堆叠修正

- TFC 原版吹制玻璃瓶结果就是：
  - `tfc:silica_glass_bottle`
  - `tfc:hematitic_glass_bottle`
  - `tfc:olivine_glass_bottle`
  - `tfc:volcanic_glass_bottle`
- `GlassBottleItem -> FluidContainerItem`
- 原版空玻璃瓶可堆叠；有流体时单个
- 已在 `GlassPressBlockEntity#prepareRecipeResult` 中对 `#tfc:glass_bottles` 清理：
  - 移除 `fluid`
  - 移除 `tfc:glass_work_data`
  - 空 tag 时 `setTag(null)`
- 用户已测试：TFC 空玻璃瓶压制输出可堆叠，没有问题

## 已验证/用户测试

- Mod 加载正常
- 创造标签正常
- `glass_press` 放置、朝向、右键 GUI 正常
- 槽位、Shift-click、NBT 保存、破坏掉落正常
- `silica_glass_batch` 可加热并压制
- 玻璃板产量已改为 16，用户测试无问题
- 陶瓷模具概率碎裂，用户测试无问题
- TFC 玻璃瓶输出可堆叠，用户测试无问题
- 其他三种玻璃批次瓶配方，用户测试无问题
- 新发现：同类模具不能堆叠；已本地修复但未提交

## Git 状态

已推送的关键提交：

- `6386bb0 Fix stackable TFC glass bottle output`
- `0c0bd85 Add bottle recipes for other glass batches`

本地未提交改动：

- `src/main/java/com/ly/tfcglassworkshop/registry/ModItems.java`
- `src/main/java/com/ly/tfcglassworkshop/item/CeramicMoldItem.java`

未提交改动内容：

- 去掉普通/钢模具的 `stacksTo(1)`
- 去掉陶瓷模具的 `stacksTo(1)`
- 编译验证：

```text
.\gradlew.bat processResources compileJava
BUILD SUCCESSFUL
```

## 下一步建议

1. 测试模具堆叠。
2. 测试无问题后提交并推送“模具可堆叠”修正。
3. 继续补其他玻璃批次的完整配方矩阵：
   - `hematitic`
   - `olivine`
   - `volcanic`
   - 对应 `plate / block / jar / lamp_glass / lens`
4. 后续可做：
   - 正式 GUI 贴图
   - 玩家物品栏背景格子
   - 模具生存合成配方
   - JEI/REI
   - Patchouli 文档
