# TFC Glass Workshop 1.21.1 移植计划书

## 1. 目标

将 `TerraFirmaCraft: Glass Workshop` 从当前的 Minecraft `1.20.1` + Forge 版本移植到 Minecraft `1.21.1` + NeoForge 版本，以适配 TerraFirmaCraft `1.21.1-4.x`。

本次移植不是小版本升级，而是一次加载器和依赖体系迁移：

- Minecraft：`1.20.1` -> `1.21.1`
- Forge：`47.x` -> NeoForge `21.1.x`
- Java：`17` -> `21`
- TFC：`1.20.1-3.x` -> `1.21.1-4.x`
- JEI：`15.x` -> `1.21.1 NeoForge 19.x`

## 2. 推荐工作方式

### 2.1 是否新建一个文件夹？

建议：**不要新建一个完全独立的新文件夹作为主开发方式**。

更推荐：

1. 继续使用当前仓库 `D:\java\TFC-Glass-Workshop`
2. 从当前稳定版本新建一个移植分支：

```powershell
git checkout -b port/1.21.1-neoforge
```

原因：

- 可以保留完整 Git 历史
- 方便对比 1.20.1 与 1.21.1 的差异
- 方便回滚某一步移植改动
- 方便以后维护两个版本线
- 不会产生两个互相脱节的项目副本

### 2.2 推荐分支结构

建议长期维护：

- `main`
  - 当前稳定线
  - Minecraft `1.20.1`
  - Forge
- `port/1.21.1-neoforge`
  - 1.21.1 移植开发线
  - NeoForge
- 后续可选：
  - `1.20.1-forge`
  - `1.21.1-neoforge`

如果未来两个版本都要长期维护，建议最终把 `main` 用作最新主要版本，把旧版维护放到 `1.20.1-forge` 分支。但在移植开始阶段，不急着调整主分支。

### 2.3 什么时候才需要新文件夹？

只有以下情况才建议复制一份新文件夹：

- 想同时在两个 IDE 窗口里并排运行两个版本
- 想保留一个完全不受 Git 分支切换影响的 1.20.1 工作目录
- NeoForge 工程结构变化太大，临时对照迁移更方便

如果需要这种工作方式，建议命名为：

```text
D:\java\TFC-Glass-Workshop-1.21.1
```

但这个文件夹应作为临时工作副本，核心代码仍应提交回同一个 Git 仓库的 `port/1.21.1-neoforge` 分支。

## 3. 目标版本与依赖

目标版本：

| 项目 | 目标 |
| --- | --- |
| Minecraft | `1.21.1` |
| Loader | NeoForge |
| Java | `21` |
| TFC | `1.21.1-4.1.0+` |
| JEI | `1.21.1 NeoForge 19.x` |
| Patchouli | 跟随 TFC 1.21.1 依赖 |

需要注意：

- TFC `1.21.1` 当前主流文件是 NeoForge 版本
- NeoForge `1.21.1` 开发环境要求 JDK 21
- 现有 Forge 1.20.1 的 `net.minecraftforge.*` API 需要迁移到 NeoForge 对应 API

## 4. 阶段计划

## 阶段一：准备移植分支

目标：创建安全的移植工作环境。

任务：

- 确认当前 `main` 已推送并保持干净
- 新建分支：

```powershell
git checkout -b port/1.21.1-neoforge
```

- 安装或确认 JDK 21
- 记录当前 1.20.1 发布 jar 和 Git tag
- 保留 `main` 作为 1.20.1 稳定线

验收标准：

- 新分支创建成功
- `main` 不受移植改动影响
- 本地可以随时切回 `main`

## 阶段二：迁移构建系统

目标：让项目变成可解析依赖的 NeoForge 1.21.1 工程。

重点文件：

- `build.gradle`
- `gradle.properties`
- `settings.gradle`
- `src/main/resources/META-INF/mods.toml`

任务：

- 将 Java toolchain 从 `17` 改为 `21`
- 将 Minecraft 版本从 `1.20.1` 改为 `1.21.1`
- 将 ForgeGradle / Forge 依赖切换为 NeoForge
- 更新 TFC 依赖到 `1.21.1-4.1.0+`
- 更新 JEI 到 `1.21.1 NeoForge 19.x`
- 检查 Patchouli 依赖是否需要显式声明
- 更新 `mods.toml` 或 NeoForge 元数据格式
- 更新运行配置：
  - `runClient`
  - `runServer`
  - 数据生成配置

验收标准：

```powershell
.\gradlew.bat tasks
.\gradlew.bat processResources
```

可以运行到可分析阶段。

## 阶段三：迁移 NeoForge / Minecraft API

目标：让 Java 代码重新编译通过。

重点模块：

- `TFCGlassWorkshop`
- `ModBlocks`
- `ModItems`
- `ModCreativeTabs`
- `ModBlockEntities`
- `ModMenuTypes`
- `ModRecipeTypes`
- `ModTags`
- `GlassPressBlock`
- `GlassPressBlockEntity`
- `GlassPressMenu`
- `GlassPressScreen`
- `PressingRecipe`
- JEI 兼容类

预计改动：

- `net.minecraftforge.*` 包迁移到 NeoForge 对应包
- `DeferredRegister` / 注册对象 API 适配
- 事件总线初始化方式适配
- Capability / ItemHandler API 适配
- Menu / Screen / Slot API 检查
- Recipe Serializer / Recipe Type API 检查
- `ResourceLocation` 创建方式检查
- Minecraft 1.21.1 item / component / tag 变化检查

验收标准：

```powershell
.\gradlew.bat compileJava
```

可以通过，或只剩 TFC API 相关报错。

## 阶段四：迁移 TFC API

目标：恢复与 TFC 的核心集成。

重点功能：

- 热量检测
- 机械动力输入
- 玻璃粉末染色
- TFC Field Guide
- TFC item size / heat 数据

需要检查的 API：

- `HeatCapability`
- `RotationSinkBlockEntity`
- `SinkNode`
- `Rotation`
- `NetworkAction`
- `GlassOperation`
- TFC recipe / data pack 格式
- TFC Field Guide / Patchouli 结构

风险最高的部分：

- 机械动力 API
- 热量 API
- `GlassOperation.getByPowder`
- Field Guide 路径与模板

验收标准：

- 编译通过
- 玻璃压制机可检测温度
- 顶部机械动力可驱动压制
- 粉末染色可识别 TFC 1.21.1 粉末

## 阶段五：迁移数据包与资源

目标：确认所有 JSON 数据在 1.21.1 下合法。

重点目录：

- `src/main/resources/data/tfc_glass_workshop/recipes/`
- `src/main/resources/data/tfc_glass_workshop/tags/`
- `src/main/resources/data/tfc_glass_workshop/tfc/item_sizes/`
- `src/main/resources/data/tfc/tfc/item_heats/`
- `src/main/resources/assets/tfc/patchouli_books/field_guide/`
- `src/main/resources/assets/tfc_glass_workshop/`

任务：

- 检查 Minecraft 1.21.1 recipe JSON 格式
- 检查 TFC 4.x item heat 数据路径
- 检查 TFC 4.x item size 数据路径
- 检查 TFC glass batch 标签是否变化
- 检查 TFC glassworking recipe 格式是否变化
- 重新抽取 TFC 1.21.1 的 glassworking 配方
- 重新生成或校验染色压制配方
- 检查输出物品 ID 是否仍然存在

验收标准：

- 启动游戏时无数据包错误
- 所有压制配方加载
- JEI 能显示压制配方
- Field Guide 能显示玻璃工坊内容

## 阶段六：运行测试

目标：确认 1.21.1 版本在游戏内可用。

测试清单：

- Mod 能正常加载
- 创造标签正常
- 玻璃压制机方块可放置
- 朝向正常
- GUI 正常
- 输入槽 / 模具槽 / 粉末槽 / 输出槽正常
- Shift-click 正常
- NBT 保存和重进世界正常
- 破坏掉落正常
- 无动力不工作
- 顶部机械动力输入有效
- 转速影响压制速度
- 普通玻璃块压制正常
- 普通玻璃板压制正常
- 玻璃瓶压制正常
- 空罐压制正常
- 灯罩压制正常
- 透镜压制正常
- 染色玻璃块压制正常
- 染色玻璃板压制正常
- 粉末顺序无关匹配正常
- 下方漏斗只抽出输出槽成品

建议重点测试染色配方：

- 红色
- 青色
- 淡灰色
- 遮光玻璃

## 阶段七：打包与发布

目标：生成可上传的 1.21.1 NeoForge jar。

任务：

- 更新版本号
- 更新 `README.md`
- 更新 `RELEASE_PAGE.md`
- 更新 `CHANGELOG.md`
- 更新项目续接摘要
- 运行：

```powershell
.\gradlew.bat build
```

- 用干净 NeoForge 1.21.1 实例测试打包 jar
- 上传 CurseForge / Modrinth 新文件

发布建议：

- 不覆盖 1.20.1 Forge 文件
- 新文件标记为：
  - Minecraft `1.21.1`
  - Loader `NeoForge`
  - Java `21`
  - TFC `4.1.0+`

## 5. 风险评估

| 风险 | 等级 | 说明 |
| --- | --- | --- |
| TFC 机械动力 API 变化 | 高 | 当前压制机依赖 `RotationSinkBlockEntity` 等 TFC 内部机制 |
| TFC 热量 API 变化 | 高 | 压制条件依赖 `HeatCapability` |
| NeoForge Capability 变化 | 中高 | 漏斗输出和自动化依赖 ItemHandler capability |
| JEI API 变化 | 中 | JEI 19.x 可能需要调整 recipe category |
| Field Guide 结构变化 | 中 | TFC 4.x 可能调整 Patchouli 目录或模板 |
| JSON 配方格式变化 | 中 | 1.21.1 数据包规则可能更严格 |
| 贴图和模型 | 低 | 大概率可直接复用 |
| 语言文件 | 低 | 大概率可直接复用 |

## 6. 时间预估

| 阶段 | 预估 |
| --- | --- |
| 分支与环境准备 | 0.5 天 |
| 构建系统迁移 | 0.5 - 1 天 |
| NeoForge API 编译修复 | 1 - 2 天 |
| TFC API 适配 | 1 - 3 天 |
| 数据包和 Field Guide 适配 | 0.5 - 1 天 |
| 游戏内测试 | 1 - 2 天 |
| 打包与发布 | 0.5 天 |

总计：约 `4 - 10` 天。

## 7. 建议的立即行动

第一步不要直接修改 `main`。

建议执行：

```powershell
git status
git checkout -b port/1.21.1-neoforge
```

然后先完成构建系统迁移，让项目能在 NeoForge 1.21.1 下解析依赖。只要构建系统站起来，后续 Java 报错就可以逐个修。

## 8. 维护建议

移植完成后建议形成两个发布线：

- `1.20.1 Forge`
  - 继续维护 bugfix
  - 不再主动加大功能
- `1.21.1 NeoForge`
  - 作为新版本主线
  - 后续新功能优先在这里开发

如果玩家仍大量使用 TFC 1.20.1，可以保留 `1.20.1-forge` 分支长期维护。
